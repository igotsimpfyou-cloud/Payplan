# PayPlan Pro (Phone-friendly)

This is a Vite + React + Tailwind project wrapping your BillPayPlanner component.

## Run locally (optional)
npm install
npm run dev

## Deploy on Vercel
Import this repo into Vercel and deploy. Then open the URL on iPhone Safari and "Add to Home Screen".
